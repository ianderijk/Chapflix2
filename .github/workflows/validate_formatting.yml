name: Validate Structure

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - "*/**"
  
jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Run structure validation
        run: python .github/scripts/validate_formatting.py